# Deploy to Vercel

1. Push your code to GitHub.
2. Import your repository on [Vercel](https://vercel.com).
3. Vercel will detect the Vite config and use the settings in `vercel.json` (build command, output directory, etc.).

Or use the Vercel CLI:

```bash
npm i -g vercel
vercel
```
